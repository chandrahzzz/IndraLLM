# core
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.66
requests>=2.31

# language id / filtering
fasttext-wheel>=0.9.2

# collection
praw>=7.7            # Reddit (free)

# LLM API (free tier — no OpenAI/Anthropic needed)
google-generativeai>=0.7

# modeling (GPU steps — also fine to install only on Colab)
torch>=2.2
transformers>=4.44
datasets>=2.20
accelerate>=0.30
bitsandbytes>=0.43   # 4-bit quantization (QLoRA / free T4)
peft>=0.11           # LoRA adapters
evaluate>=0.4
scikit-learn>=1.4

# auto-labeling
bert-score>=0.3.13

# API backend
fastapi>=0.110
uvicorn>=0.29

# logging / publish
wandb>=0.17
huggingface_hub>=0.24
