[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "indrallm"
version = "0.1.0"
description = "Code-switched hallucination benchmark and mitigation for Indian languages"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
